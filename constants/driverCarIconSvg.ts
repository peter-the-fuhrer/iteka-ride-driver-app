/**
 * Driver marker icon (car) used on the map for the driver's position.
 * Paste your full <svg>...</svg> markup below (same as dashboard driver-car-icon).
 * Leave as empty string to keep the pulsing circle until you paste.
 */
export const DRIVER_CAR_SVG = ``;
